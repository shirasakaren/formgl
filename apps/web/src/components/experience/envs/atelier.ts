'use client';
import { parkIntro, parkOpen, parkSend } from '../timeline';
// temporary: replaced by the atelier choreography
export const atelierIntro = parkIntro;
export const atelierOpen = parkOpen;
export const atelierSend = parkSend;
