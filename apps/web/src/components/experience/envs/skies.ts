'use client';
import { parkIntro, parkOpen, parkSend } from '../timeline';
// temporary: replaced by the skies choreography
export const skiesIntro = parkIntro;
export const skiesOpen = parkOpen;
export const skiesSend = parkSend;
